[tool:pytest]
testpaths = tests
asyncio_mode = auto

[coverage:run]
source = app
omit =
    app/config/*
    app/alembic/*
    */migrations/*
    */__pycache__/*

[coverage:report]
show_missing = true
skip_covered = false
fail_under = 0

[coverage:xml]
output = coverage.xml
